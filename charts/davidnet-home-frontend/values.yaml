replicaCount: 1

image:
  repository: ghcr.io/davidnet-net/home
  pullPolicy: Always
  tag: "latest"

service:
  type: ClusterIP
  port: 3000

ingress:
  enabled: true
  host: home.davidnet.net
  ingressClass: traefik
